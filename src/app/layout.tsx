import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Project Tasklist - Manage Your Projects',
  description: 'Efficiently manage your projects and tasks with progress tracking and status management.',
  keywords: 'project management, task tracker, project tracker, tasklist',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="id">
      <body className={inter.className}>{children}</body>
    </html>
  )
}